version: "1"
project:
    id: ""
packages:
  - name: "postgres" # 出力ディレクトリ名
    path: "postgres" # 出力ディレクトリ
    queries: "query.sql" # クエリの初期化
    schema: "schema.sql" # スキーマの初期化
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_exact_table_names: false
