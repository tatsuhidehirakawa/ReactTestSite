version: "1"
project:
    id: ""
packages:
  - name: "build_sqlc" # 出力ディレクトリ名
    path: "build_sqlc" # 出力ディレクトリ
    emit_json_tags: true
    engine: "postgresql"
    schema: "schema.sql" # スキーマの初期化
    queries: "query.sql" # クエリの初期化